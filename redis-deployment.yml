apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-deployment
  labels:
    app: demo-voting-app
    name: redis-deployment
spec:
  replicas: 1
  template:
    metadata:
      name: redis-pod
      labels:
        name: redis-pod
        app: demo-voting-app
    spec:
      containers:
      - name: redis-container
        image: redis
        ports:
        - containerPort: 6379
  selector:
    matchLabels:
      name: redis-pod
      app: demo-voting-app
